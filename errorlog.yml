---
- hosts: all
  gather_facts: true
  become: yes
  vars:
      ansible_become: true
  
  tasks:
    - name: Gather file
      command: cat error.log chdir=/var/log/apache2
      register: errorlog
    
    - name: Print log
      debug:
        msg: "{{errorlog.stdout}}"